# Copy to .env and fill in. .env is gitignored; this file is not, so keep it
# to key names only — never paste a real value here.
#
# Every key a tool needs must be listed here, so a fresh checkout knows what
# to provide. Read them with require_env("KEY") from tools/_common.py.

# EXAMPLE_API_KEY=
