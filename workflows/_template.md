# <Workflow name>

**Objective**
What this accomplishes and when to run it.

**Inputs**
- `<name>` — what it is, where it comes from, what a valid value looks like.

**Tools**
- `tools/<name>.py` — what it does. Note here if it costs money or credits.

**Steps**
1. ...
2. ...

**Output**
Where the result lands (cloud destination for deliverables, `.tmp/` for
intermediates) and what it should look like.

**Edge cases and constraints**
Rate limits, auth quirks, timing, known failure modes. Add to this section
every time a run teaches you something — that is the point of the file.
